<form method="post" action="{{ path('app_meo_produit_delete', {'id': meo_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meo_produit.id) }}">
    <button class="btn">Supprimer</button>
</form>
