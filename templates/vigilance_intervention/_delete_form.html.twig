<form method="post" action="{{ path('app_vigilance_intervention_delete', {'id': vigilance_intervention.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vigilance_intervention.id) }}">
    <button class="btn">Supprimer</button>
</form>
