{% extends 'base.html.twig' %}

{% block title %}VigilanceIntervention{% endblock %}

{% block body %}
    <h1>VigilanceIntervention</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vigilance_intervention.id }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ vigilance_intervention.detail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vigilance_intervention_index') }}">Retour a la liste</a>

    <a href="{{ path('app_vigilance_intervention_edit', {'id': vigilance_intervention.id}) }}">Modifier</a>

    {{ include('vigilance_intervention/_delete_form.html.twig') }}
{% endblock %}
