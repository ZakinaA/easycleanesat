<form method="post" action="{{ path('app_support_client_delete', {'id': support_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ support_client.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
