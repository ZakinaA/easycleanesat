{% extends 'base.html.twig' %}

{% block title %}Plage{% endblock %}

{% block body %}
    <h1>Plage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plage.id }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ plage.heureDebut ? plage.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Heure de fin</th>
                <td>{{ plage.heureFin ? plage.heureFin|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plage_index') }}">Retour a la liste</a>

    <a href="{{ path('app_plage_edit', {'id': plage.id}) }}">Modifier</a>

    {{ include('plage/_delete_form.html.twig') }}
{% endblock %}
