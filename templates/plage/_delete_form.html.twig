<form method="post" action="{{ path('app_plage_delete', {'id': plage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plage.id) }}">
    <button class="btn">Supprimer</button>
</form>
