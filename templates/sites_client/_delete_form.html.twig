<form method="post" action="{{ path('app_sites_client_delete', {'id': sites_client.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce site client ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sites_client.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
