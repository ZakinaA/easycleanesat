<form method="post" action="{{ path('app_moyen_dosage_delete', {'id': moyen_dosage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moyen_dosage.id) }}">
    <button class="btn">Supprimer</button>
</form>
