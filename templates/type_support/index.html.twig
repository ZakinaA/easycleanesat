{% extends 'base.html.twig' %}
{% from '_macros/action_buttons.html.twig' import action_buttons %}
{% block title %}Types de support
{% endblock %}
{% block body %}
	<div class="page-header">
		<h1>Types de support</h1>
		{% if app.user and is_granted('ROLE_ADMIN') %}
			<a href="{{ path('app_type_support_new') }}" class="btn btn-primary">+ Ajouter un type</a>
		{% endif %}
	</div>
	<div class="table-wrapper">
		<table>
			<thead>
				<tr>
					<th>#</th>
					<th>Picto</th>
					<th>Nom</th>
					<th>Description</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for type_support in type_supports %}
					<tr>
						<td>{{ loop.index }}</td>
						<td>
							{% if type_support.picto %}
								<img src="{{ asset('PictoTypeSupportPNG/' ~ type_support.picto) }}" alt="{{ type_support.nom }}" style="max-height: 40px;">
							{% else %}
								-
							{% endif %}
						</td>
						<td>{{ type_support.nom }}</td>
						<td>{{ type_support.description ?? '' }}</td>
						<td>
							{{ action_buttons('app_type_support_show', 'app_type_support_edit', type_support.id, true) }}
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="5">Aucun type de support trouve</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
